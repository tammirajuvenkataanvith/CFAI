# CFAI
⚡ Interactive Streamlit benchmark app comparing Linear Search (O(N)), Binary Search (O(log N)), and Hash-Set Lookup (O(1)) on datasets up to 5 million elements. Includes real-time timing, complexity analysis, and custom SVG visualizations built without Plotly or Matplotlib.
