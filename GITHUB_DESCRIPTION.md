## GitHub Repository Description (paste into the About box)

> ⚡ Interactive Streamlit benchmark lab comparing O(N) linear search, O(log N) binary search, and O(1) hash-set lookup on datasets up to 5 million elements — hand-drawn SVG charts, no Plotly.

---

## Topics / tags (add in GitHub → Settings → Topics)

```
algorithm  benchmark  search  streamlit  python  data-structures
big-o  complexity  visualization  svg  linear-search  binary-search
hash-table  educational  interactive
```

---

## Social preview text (for LinkedIn / X share)

> Built a live algorithm benchmark app in pure Streamlit — O(N) linear search, O(log N) binary search, and O(1) hash-set lookup, timed on real hardware up to 5 M elements. Every chart is hand-drawn SVG; no Plotly, no Matplotlib. #Python #Algorithms #Streamlit
